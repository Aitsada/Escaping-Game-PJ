# Survive

Vite canvas game.

## Run locally

Install dependencies:

```sh
npm install
```

Start the dev server:

```sh
npm run dev
```

## Deploy

Build the static output:

```sh
npm run build
```

Deploy the generated `dist/` folder.
